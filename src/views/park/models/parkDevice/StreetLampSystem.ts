import {TKBaseObject} from "@/utils/TKThree/TKBaseObject";
import * as THREE from 'three';

/**
 * 街灯系统
 */
export class StreetLampSystem extends TKBaseObject {
  constructor(config: any, model: THREE.Object3D) {
    super(config, model);
  }

  override start() {

  }

  override update(delta: number) {

  }
}
