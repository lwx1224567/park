import * as THREE from 'three';
import {TKBaseObject} from '@/utils/TKThree/TKBaseObject';

/**
 * 园区/阵地
 */
class Back extends TKBaseObject {
  constructor(config, model) {
    super(config, model);

  }

  override start() {
  }

  override update(delta: number) {
  }

}

export {Back};
