import { withInstall } from '@/utils';

import leftPopup from './src/LeftPopup.vue';
import rightPopup from './src/RightPopup.vue';

export const LeftPopup = withInstall(leftPopup);
export const RightPopup = withInstall(rightPopup);
